# テスト実行手順書

## 事前準備

### 1. Supabaseでのデータベース設定

SupabaseのSQL Editorで以下を順番に実行：

```sql
-- 1. user_settingsテーブル作成
CREATE TABLE IF NOT EXISTS user_settings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  line_notify_token TEXT,
  line_notify_enabled BOOLEAN NOT NULL DEFAULT false,
  gemini_api_key TEXT,
  gemini_enabled BOOLEAN NOT NULL DEFAULT false,
  notification_time TIME NOT NULL DEFAULT '20:00:00',
  created_at TIMESTAMP NOT NULL DEFAULT now(),
  updated_at TIMESTAMP NOT NULL DEFAULT now(),
  UNIQUE(user_id)
);

CREATE INDEX IF NOT EXISTS idx_user_settings_user_id ON user_settings(user_id);

-- 2. RLS設定
ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own settings" ON user_settings
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own settings" ON user_settings
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own settings" ON user_settings
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own settings" ON user_settings
  FOR DELETE USING (auth.uid() = user_id);

-- 3. 年収データ移行
UPDATE profiles
SET income = CASE
  WHEN income::integer < 300 THEN '〜300万'
  WHEN income::integer >= 300 AND income::integer < 500 THEN '300〜500万'
  WHEN income::integer >= 500 AND income::integer < 700 THEN '500〜700万'
  WHEN income::integer >= 700 AND income::integer < 1000 THEN '700〜1000万'
  WHEN income::integer >= 1000 AND income::integer < 1500 THEN '1000〜1500万'
  WHEN income::integer >= 1500 AND income::integer < 2000 THEN '1500〜2000万'
  WHEN income::integer >= 2000 AND income::integer < 3000 THEN '2000〜3000万'
  WHEN income::integer >= 3000 AND income::integer < 5000 THEN '3000〜5000万'
  WHEN income::integer >= 5000 THEN '5000万以上'
  ELSE NULL
END
WHERE income IS NOT NULL 
  AND income ~ '^[0-9]+$';
```

### 2. テストユーザーの作成

Supabaseの認証画面で以下のユーザーを作成：

- Email: `test@example.com`
- Password: `testpassword`

### 3. テストデータの投入

```sql
-- テスト用プロフィール（IDは自動生成されるUUIDを使用）
INSERT INTO profiles (user_id, name, age, status, app, income, summary)
VALUES 
  ((SELECT id FROM auth.users WHERE email = 'test@example.com'), '山田花子', 28, '本命', 'Pairs', '500〜700万', '明るくて優しい人'),
  ((SELECT id FROM auth.users WHERE email = 'test@example.com'), '佐藤美咲', 25, 'あり', 'Omiai', '300〜500万', '趣味が合う'),
  ((SELECT id FROM auth.users WHERE email = 'test@example.com'), '鈴木愛', 30, '終了', 'Tinder', NULL, NULL);

-- テスト用イベント（profile_idは上で作成されたプロフィールのIDを使用）
INSERT INTO events (user_id, profile_id, event_date, comment)
SELECT 
  (SELECT id FROM auth.users WHERE email = 'test@example.com'),
  p.id,
  '2026-03-01',
  '楽しかった'
FROM profiles p
WHERE p.user_id = (SELECT id FROM auth.users WHERE email = 'test@example.com')
  AND p.name = '山田花子'
LIMIT 1;

INSERT INTO events (user_id, profile_id, event_date, comment)
SELECT 
  (SELECT id FROM auth.users WHERE email = 'test@example.com'),
  p.id,
  '2026-02-15',
  NULL
FROM profiles p
WHERE p.user_id = (SELECT id FROM auth.users WHERE email = 'test@example.com')
  AND p.name = '佐藤美咲'
LIMIT 1;

-- テスト用user_settings（LINE未連携）
INSERT INTO user_settings (user_id, line_notify_enabled, gemini_enabled)
VALUES ((SELECT id FROM auth.users WHERE email = 'test@example.com'), false, false)
ON CONFLICT (user_id) DO NOTHING;
```

### 4. テスト用画像の準備

`tests/fixtures/test-image.jpg` に適当な画像ファイルを配置

## テスト実行

### 手動テスト

1. **ローカルサーバー起動**
```bash
# VSCodeのLive Serverを使用
# または
npx http-server -p 5500
```

2. **ブラウザで確認**
   - http://localhost:5500/login.html にアクセス
   - test@example.com / testpassword でログイン
   - 各画面を確認

### 自動テスト（Playwright）

1. **テスト実行**
```bash
cd c:\Users\pon10\OneDrive\デスクトップ\tenkai
npm test
```

2. **特定のテストのみ実行**
```bash
# ホーム画面のテストのみ
npx playwright test home.spec.ts

# アカウント管理のテストのみ
npx playwright test account.spec.ts

# 画像アップロードのテストのみ
npx playwright test image-upload.spec.ts
```

3. **UIモードで実行**
```bash
npx playwright test --ui
```

4. **デバッグモード**
```bash
npx playwright test --debug
```

## テストチェックリスト

### ホーム画面
- [ ] HOME-001: 概要カードの表示（PC）
- [ ] HOME-002: 概要カードの表示（スマホ）
- [ ] HOME-003: 登録相手数の表示
- [ ] HOME-006: LINE未連携時のアナウンス表示
- [ ] HOME-009: Gemini未設定時のメッセージ
- [ ] HOME-011: ステータス別グラフの表示
- [ ] HOME-012: 月別推移グラフへの切り替え
- [ ] HOME-013: アプリ別グラフへの切り替え

### アカウント管理
- [ ] ACCT-001: LINE未連携状態の表示
- [ ] ACCT-003: LINE連携解除
- [ ] ACCT-004: LINE連携解除キャンセル
- [ ] ACCT-005: Gemini設定の読み込み
- [ ] ACCT-007: Gemini設定の保存
- [ ] ACCT-008: Gemini無効化

### 画像アップロード
- [ ] PROF-001: 画像選択（PC）
- [ ] PROF-002: トリミング表示（スマホ）
- [ ] PROF-003: トリミング決定
- [ ] PROF-004: トリミング後の保存

## トラブルシューティング

### テストが失敗する場合

1. **ローカルサーバーが起動していない**
   - Live Serverまたはhttp-serverを起動してください

2. **テストユーザーが存在しない**
   - Supabaseで test@example.com を作成してください

3. **データベーステーブルが存在しない**
   - SQLを実行してテーブルを作成してください

4. **画像ファイルが見つからない**
   - tests/fixtures/test-image.jpg を配置してください

### エラーログの確認

```bash
# 詳細なログを表示
npx playwright test --reporter=list

# HTMLレポートを生成
npx playwright test --reporter=html
npx playwright show-report
```

## テスト後のクリーンアップ

```sql
-- テストデータの削除（名前で識別）
DELETE FROM events 
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'test@example.com');

DELETE FROM profiles 
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'test@example.com')
  AND name IN ('山田花子', '佐藤美咲', '鈴木愛');

DELETE FROM user_settings 
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'test@example.com');
```
